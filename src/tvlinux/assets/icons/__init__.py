"""Bundled SVG icons.

Sourced from Lucide (https://lucide.dev), MIT licensed. Each icon is
normalised to a 24x24 viewBox with ``currentColor`` stroke so it picks up
the Qt palette at render time where possible, or falls back to the neutral
secondary-text color hard-coded in the SVG for toolbar use.
"""
