# Runtime Python dependencies bundled into the Flatpak.
# Kept in sync with pyproject.toml's [project].dependencies.
PySide6>=6.7
dbus-next>=0.2.3
PyGObject>=3.48
platformdirs>=4.2
structlog>=24.1
numpy>=1.26
